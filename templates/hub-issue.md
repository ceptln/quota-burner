# Autopilot hub

Label this issue `autopilot-hub`; exactly one open hub must exist. This is the live state for `.quota-burner/CONTRACT.md`. Add tasks with the `todo` skill or edit the queue directly.

## To-do

Add unchecked items here, oldest first. Include paths, intent, and evidence for a session with no prior context. No sample task is queued by this template.

## Chantiers

Link each multi-run effort with its state and progress. Use the chantier template and label `autopilot-chantier`.

## Scheduler

Missing lines mean `ok`. Add per-skill lines as needed: `- SKILL: ok`, `- SKILL: boost`, or `- SKILL: snooze-until YYYY-MM-DD`, optionally followed by `| note: HANDOFF`. Only humans and retro set boosts; snoozes last at most 30 days.

## Scorecard

Swept through: never

Retro maintains per-skill counts and unresolved PR identities here. `never` means the initial sweep covers all autopilot PRs.

## Status

Not started.
