---
name: "pattern-align"
description: "Use to align code on the house design patterns and conventions, one pattern times one module per run. Chantier: needs an approved chantier issue."
---

Read `.quota-burner/CONTRACT.md` and the configured team context before targeting. Standalone invocation follows repository permissions and does not authorize a PR by itself.

Align code on the house patterns, one pattern times one module per run. The patterns ARE your guidelines: this skill never invents them.

Targeting: read your guidelines files; the first run inventories violations per module into the chantier issue, later runs execute one batch each.

Done: the batch module fully aligned; chantier progress updated.
