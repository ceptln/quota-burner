---
name: "test-gap-fill"
description: "Use to add behavior tests to the least-covered module."
---

Read `.quota-burner/CONTRACT.md` and the configured team context before targeting. Standalone invocation follows repository permissions and does not authorize a PR by itself.

Add behavior tests to ONE under-covered module.

Targeting: the coverage report; your guidelines define testing style (behavior over implementation).

Done: coverage delta reported; tests assert behavior, not implementation.
