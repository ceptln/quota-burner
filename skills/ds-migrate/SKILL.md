---
name: "ds-migrate"
description: "Use to migrate ad hoc frontend components to the design system, one batch per run. Chantier: needs an approved chantier issue."
---

Read `.quota-burner/CONTRACT.md` and the configured team context before targeting. Standalone invocation follows repository permissions and does not authorize a PR by itself.

Migrate ad hoc components to the design system, one batch per run.

Targeting: your design-system conventions (guidelines or the design-system package itself); the first run inventories off-system components into the chantier issue.

Done: batch migrated with visual parity; chantier progress updated.
