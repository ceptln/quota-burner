---
name: "primitives-align"
description: "Use to fold components that reimplement their own variants (buttons, modals, hardcoded spacing or colors) onto the shared primitives. Chantier: needs an approved chantier issue."
---

Read `.quota-burner/CONTRACT.md` and the configured team context before targeting. Standalone invocation follows repository permissions and does not authorize a PR by itself.

Fold divergent component variants onto the shared primitives, one batch per run.

Targeting: your design system defines the primitives; the first run inventories divergent variants into the chantier issue.

Done: batch folded with visual parity; chantier progress updated.
